{% extends 'layouts/default.j2' %}}
{% block content %}
	<h1 class="postitle">{{ post.title }}</h1>
	<hr>
	{{ post.content|safe }}
	<hr>
	<p class="postdate">Le <span class="date">{{ post.datecr }}</span> (update <span class="date">{{ post.dateup }}</span>)</p>
{% endblock %}